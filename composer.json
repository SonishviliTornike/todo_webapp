{
    "name": "tornike/todo_webapp",
    "description": "To do web application",
    "type": "project",
    "require" : {
        "php" : ">=8.2"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "analyse": "phpstan analyse"
    },
    "require-dev": {
        "phpstan/phpstan": "^2.2"
    }
}
